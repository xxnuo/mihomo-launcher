package res

const (
	Separator = `/` // the path separator of the windows used in res.Join

	RootFolder    = "root" // root res folder name
	UIFolderName  = "ui"   // ui res folder name
	BinFolderName = "bin"  // bin res folder name

	InstallServiceExeName   = "install-service.exe"     // install service exe name
	UninstallServiceExeName = "uninstall-service.exe"   // uninstall service exe name
	ServiceExeName          = "clash-verge-service.exe" // service exe name
)
